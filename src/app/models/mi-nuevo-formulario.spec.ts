import { MiNuevoFormulario } from './mi-nuevo-formulario';

describe('MiNuevoFormulario', () => {
  it('should create an instance', () => {
    expect(new MiNuevoFormulario()).toBeTruthy();
  });
});
